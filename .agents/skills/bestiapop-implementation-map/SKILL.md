---
name: bestiapop-implementation-map
description: >-
  Mapa vivo de implementaciones BestiaPop: archivos, clases y funciones clave
  por concern. Usar para localizar código rápido, citar entry points en PRs o
  al añadir features. Actualizar cuando se creen, renombren o muevan símbolos
  importantes.
---

# BestiaPop — Mapa de implementaciones

Paths relativos a `app/src/main/java/com/bestiapop/android/`.

## Entrada app

| Concern | Archivo |
|---------|---------|
| Application + process graph | `BestiaPopApplication.kt` (`BestiaPopApplication.onCreate`: `MusicRepository`; radio engine; `ProcessDownloadCoordinator.create`; `ProcessDownloadRuntime.create`; `ProcessIdentifyRuntime.create`; `ProcessSaveWhileListeningCoordinator`; `PlaybackRuntime.create`; `newImageLoader` Coil RGB565 + decoder parallelism 2) |
| Crash non-fatals / keys | `data/util/CrashReporter.kt` (`setKey` / `log` / `recordNonFatal`; identify batch keys `identify_high`/`identify_medium`/`identify_low`/`identify_none`/`identify_skipped`/`identify_lb_hits`) |
| Activity | `MainActivity.kt` (`enableEdgeToEdge`; `onStart` → `MusicPlayerViewModel.onAppForeground` probe+resume en IO; unknown-sources launcher) |
| Manifest / permisos / services | `app/src/main/AndroidManifest.xml` (`MusicService` + `MediaButtonReceiver`; Android Auto `@xml/automotive_app_desc`; FGS permissions; FileProvider; backup rules) |
| Reglas de backup | `res/xml/data_extraction_rules.xml` (API 31+, `<cloud-backup>` excluye Room + `library_settings` + token LB; `<device-transfer>` permite casi todo) y `res/xml/backup_rules.xml` (API ≤ 30). Restaurar Room sin los archivos de `Music/BestiaPop` deja filas irreproducibles, y el flag `initial_library_scan_completed` restaurado impide el rescan que las reconstruye |
| Gradle app | `app/build.gradle.kts` (`signingConfigs.release`, debug+release same cert si hay keystore, `versionCode`/`versionName` desde `version.properties`, `BuildConfig.GITHUB_REPOSITORY` desde `github-release.properties`, `targetSdk` 36, release R8 + resource shrinking + `ndk.debugSymbolLevel`, Firebase Crashlytics sin Analytics); catálogo `gradle/libs.versions.toml` (`media3 = 1.11.0`) |
| R8 keep rules | `app/proguard-rules.pro` (keep `com.bestiapop.android.**`, Ktor/Room; mapping en AAB) |
| Versión release | `version.properties` (`VERSION_CODE` / `VERSION_NAME`; bump en `release.sh` vía `scripts/version.sh`) |
| Repo GitHub Releases | `github-release.properties` (`GITHUB_REPOSITORY` → `BuildConfig.GITHUB_REPOSITORY`) |
| Deploy dispositivo | `install.sh` (`--debug` default, `--release`; `adb install -r -d`, fallback `cmd package uninstall -k`; `appops RUN_ANY_IN_BACKGROUND allow`) |
| Diagnóstico bloqueo | `scripts/check-locked-playback.sh` (no instala/borra: comprueba PID, MediaSession, FGS y notif 1001 con pantalla apagada) |
| Deploy GitHub Releases | `release.sh` (bump, validación tag + `resolve_release_notes` antes de buildear, `assembleRelease`, `dist/BestiaPop-*.apk`, `gh release create --latest [--target commit]`; notas: `--notes-file` / `--notes` / `CHANGELOG.release-notes.md` / plantilla, `ensure_notes_have_body` + `ensure_version_code_in_notes`; `verify_published_release` post-publish) |
| Deploy Play Console | `deploy-play.sh` (legacy; track default `alpha`, bump vía `scripts/version.sh`, ícono `play/icon.png` → mipmap, `bundleRelease`, `dist/*.aab`, `--upload --rollout`) |
| Ícono launcher / Play hi-res | `play/icon.png` (PNG 512×512; placeholder; `AndroidManifest` `@mipmap/ic_launcher`) |
| Play feature graphic | `play/feature-graphic.png` (1024×500 RGB, ficha Console; no va en el AAB) |
| Release keystore template | `keystore.properties.example` |
| Play API service account | `play-service-account.json.example` → JSON descargado de Cloud (IAM → Keys), no de Play Console UI |

## UI — screens

| Pantalla | Archivo |
|----------|---------|
| Shell + bottom nav | `ui/screens/MainScreen.kt` (`SaveableStateHolder` preserva estado/scroll de pestañas; `downloadBadgeCount` vía `activeDownloadBadgeCount`; `ConfirmMergeAlbumsDialog` vía `pendingAlbumMerge`; `AppUpdateDialogs` + check al abrir; banner `blocksBackgroundPlayback` + `restrictionGuidance`; diálogo OEM solo si `oemScreenOffCleanupActive`) |
| System back (exit doble + orquestación) | `MainScreen` `BackHandler` + `SnackbarHost`; nested en screens abajo; identify review overlay |
| Biblioteca | `ui/screens/LibraryScreen.kt` (`BackHandler`: multi-select / addition / album-artist-genre / search; Tune → `LibraryBrowseSortSheet`; chips `LibraryFilterChipRow`; `PlayShuffleIconPair`; `LibraryBrowsePane` + `LibrarySongListHost` + `rememberSongActionDialogs`; buscador con sugerencias `SearchRecentChipsRow`, historial `SearchHistorySheet`, acción de teclado IME y guardado al interactuar) |
| Identify review | `ui/screens/IdentifyReviewScreen.kt` (`IdentifyReviewOverview`, `IdentifyAlbumGroupCard`, `IdentifyCandidateRow` + diffs `identifyApplyChanges`, `IdentifyApplyFieldsChips`, `IdentifySearchBlock` + filtros sembrados, `IdentifyLoadMoreButton`; Aplicar automático / Omitir todas / Aplicar grupo; overlay en `MainScreen`) |
| Lista canciones / álbumes / artistas / géneros | `ui/screens/library/LibrarySongList.kt` (`currentSongIdFlow` + highlight `derivedStateOf`; `filterCollapsedAlbumSongs` / `AlbumHeader.matchesCollapsed` / `albumHeaderSelectionState`; Coil async artwork + `rememberLazyListState`); `ui/components/FastScrollScrubber.kt` (`FastScrollLazyColumn` L2 scaffold, `FastScrollContainer`, `FastScrollDefaults.dedicatedStartGutterWidth` / `dedicatedEndGutterWidth` con margen dedicado lateral según la posición activa, `FastScrollScrubber`, `FastScrollRail`, `FastScrollIndicatorBubble`, `FastScrollSections` adaptativo con tick háptico, orden natural A-Z / números / símbolos y thumb de posición); `LibrarySongListHost.kt` (`LibrarySongListActions` `@Immutable` componiendo `songActions` y `albumActions: LibraryAlbumGroupActions`, constructor L1 retrocompatible, `rememberSongActionDialogs` / `SongActionDialogsHost`, `AlbumEditDialogsHost`), `LibraryFilterChipRow.kt`, `LibraryBrowseSortSheet.kt` (`libraryOrderSummary`, `SortOption.sortLabel`, `sortDirectionLabel`, `albumHeadersActive`), `LibraryAlbumBrowseList.kt` (`AlbumHeader` L2 + `FastScrollLazyColumn` + `AlbumBrowseActions`), `AlbumEditCoverMenuItems.kt`, `LibraryAggregateListItem.kt` (fila artista/género + `AggregateBrowseActions`), `LibraryArtistList.kt` (`FastScrollLazyColumn` + `AggregateBrowseActions`), `LibraryGenreList.kt` (`FastScrollLazyColumn` + `AggregateBrowseActions`), `LibraryDialogs.kt` (`EditSongMetadataDialog` Nº de pista, `EditAlbumMetadataDialog`, `ConfirmMergeAlbumsDialog`, `SetAlbumArtworkDialog`, `AddToPlaylistDialog` con soporte multi-selección, creación inline y abrir playlist), `ui/components/PlaylistFormDialog.kt` (`PlaylistFormDialog` con acciones Crear y "Crear y entrar"), `EditLyricsDialog.kt` (tabs Texto/Sincronizar + playback), `LibraryProgressBanner.kt` + `IdentifyPendingBanner` |
| Descubrir | `ui/screens/discover/DiscoverScreen.kt` (`DiscoverTopSearchBar` con acción de historial, `DiscoverCategoryChipsRow`, `DiscoverRecentSearchesView` delegando a `SearchRecentChipsRow`, `SearchHistorySheet` para ver todo el historial, `DiscoverHomeFeedView`, `DiscoverSearchResultsView`, `DiscoverCollectionDetailView`, `DiscoverArtistDetailSection` L3 aísla canciones/álbumes locales de Room solo en vista de artista, `DiscoverArtistDetailView` L1/L2 con proporciones corregidas `cardWidth = 150.dp, imageSize = 134.dp` en álbumes locales, `DiscoverCollectionActions`, `DiscoverAdvancedFiltersPanel` delegando a `CatalogAdvancedFiltersPanel`, `DiscoverTrackListItem`, `ItemLibraryStatusIcon`), `DiscoverLbPlaylists.kt` (`DiscoverMatchedTrackActions`, `MatchedPlaylistContent`, `CfRecommendationsDetailScreen`, `LbPlaylistDetailScreen`, `DiscoverPlaylistDetailHost`); `ui/components/CatalogAdvancedFiltersPanel.kt` (`CatalogAdvancedFiltersPanel` L1/L2); `ui/components/DownloadActionWidgets.kt` (`ActiveAlbumDownloadProgress`, `findAlbumDownloadProgress`); `ui/state/CatalogSearchCoordinator.kt` (`searchMore`); `data/network/MetadataFetcher.kt` (`fetchArtistAlbums`, `fetchArtistTopTracks`) |
| Playlists | `ui/screens/PlaylistsScreen.kt` (`searchQuery` filter; integrado como chip `PLAYLISTS` en `LibraryScreen`; `BackHandler`: CF → LB → local detail; filas con `PlaylistHeader` estilo `AlbumHeader`, botones rápidos de reproducción y aleatorio, menú ⋮ con edición inline `PlaylistFormDialog`, eliminación y encolar; tap en cabecera abre detalle; play/shuffle vía `LabeledPlayShuffleButtons` + `playCollection`/`shuffleCollection`; `MatchedTrackLazyColumn`; `matchedStreamCountLabel`; `rememberSongQueueActions`; soporte para canciones duplicadas, modo "Mover canciones" para reordenar interactivamente vía drag handle `Icons.Default.DragHandle` + persistencia en Room vía `MusicDao.reorderPlaylistSongs`; creación vía `PlaylistFormDialog` con opción de entrar directo; `PlaylistPendingTrackItem` aísla observación de descargas pendientes) |
| Ajustes / ListenBrainz | `ui/screens/SettingsScreen.kt` (`BackHandler` sección; home **Actualización** (sección `Update`) + **Invitar amigos** → `/releases/latest`; `VERSION_NAME`), `ListenBrainzSettingsScreen.kt` (configuración de scrobbling y fuente de recomendaciones para Discover) |
| Ajustes / Biblioteca | `ui/screens/LibrarySettingsScreen.kt` (`SettingsScreen` sección `Library`; configuración de qué blobs/categorías mostrar en biblioteca y reordenamiento interactivo vertical drag & drop vía `LibraryBlobsSettings` + `rememberVerticalReorderDrag` y asignación del principal; scroll rápido vertical on/off + borde derecho/izquierdo para zurdos vía `FastScrollSettings`; gestos en submenús on/off swipe back y selector de acción personalizada swipe left vía `SubmenuGesturesSettings`) |
| App update UI | `ui/update/AppUpdateViewModel.kt` (`AppUpdateDependencies`: gateway/store/clock/debug/installer inyectables; constructor `Application` conserva factory; `maybeCheckOnLaunch` / `refreshReleases` / `startUpdate` / `confirmUpdate` / `launchInstaller`; `AppUpdateUiState` instalación + `AppReleaseNotesState` notas); `ui/update/AppUpdateScreen.kt` (Ajustes → Actualización); `ui/update/AppUpdateDialogs.kt` (`Available` / `Downloading` / `Error`) |
| Ajustes / Reproducción | `ui/screens/PlaybackSettingsScreen.kt` (`SettingsScreen` sección `Playback`; switch abrir reproductor al reproducir; sección crossfade con switch y slider de duración en segundos/ms; slider online; Batería separa `blocksBackgroundPlayback` vs Doze; copy `backgroundRestrictionGuidance`; botón OEM `openOemScreenOffCleanupSettings` solo si `oemScreenOffCleanupActive`) |
| Ajustes / Sonido | `ui/screens/VolumeBoostSettingsScreen.kt` (`SettingsScreen` sección `Sound`) |
| Ajustes / Descargas | `ui/screens/DownloadSettingsScreen.kt` (`SettingsScreen` sección `Downloads`; metered + path + totales bytes; deep-link `pendingSettingsSection`) |
| Ajustes / Archivos | `ui/screens/LibraryTagWriteSettingsScreen.kt` (`SettingsScreen` sección `LibraryTags`; auto-write toggle + batch sync) |
| Ajustes / Telemetría | `ui/screens/TelemetrySettingsScreen.kt` (`SettingsScreen` sección `Telemetry`; toggle envío métricas técnicas de estabilidad y diagnósticos anónimos; tarjetas explicativas de qué se recolecta, cuándo se sube y garantías de privacidad) |
| Ajustes / Letras | `ui/screens/LyricsSettingsScreen.kt` (`SettingsScreen` sección `Lyrics`; switch guía fonética, selector de modo japonés Rōmaji vs Hiragana y toggle de confirmación antes de Google Traductor) |
| Now playing | `ui/screens/NowPlayingScreen.kt` (`BackHandler` → `onDismiss`; `NowPlayingTabSelector` píldora Portada/Letra + `HorizontalPager`; Portada con scroll continuo a cola + `NowPlayingDockedBar` flotante; Letra a pantalla completa `NowPlayingLyricsView` con auto-scroll sincronizado, timestamps interactivos, tap `seekToAndPlay`, toggle de traducción, atribución con link externo y renderizado dual primary/secondary con `DisplayLyricLine`; ⋮ `NowPlayingActionsMenu` con `matchedAlbum`/`matchedArtist` derivados con `distinctUntilChanged`; remoto `NowPlayingRemoteDownloadButton` L2 / `NowPlayingRemoteDownloadAction` L1; hero `ArtworkHero`; controles unificados `NowPlayingControlsRow`) |
| Cola | `ui/screens/QueueScreen.kt` (`QueueLazyList` + `displayQueue`, drag → `moveDisplayQueueItem`, swipe right `SwipeToDismissBox` para remover de la cola) |
| WiFi sync y Añadir | `ui/screens/WebServerScreen.kt` (`WebServerScreen(viewModel, onSelectFolderClick, onOpenDownloads)` + tabs WiFi Sync / Carpeta local / Por enlace + transferencias + botón conflictos + `rememberSongActionDialogs`) |
| Temas | `ui/screens/ThemeSettingsScreen.kt` (hero de modo dinámico por canción con swatches en vivo; acceso a editor de temas con preview de biblioteca y lista de presets) |
| Theme harmonizer | `ui/theme/ThemeHarmonizer.kt` (WCAG 2.1 contraste y luminancia, conversiones HSL, generación completa ColorScheme Material 3 y auto-armonización) |
| Dynamic theme engine | `ui/theme/DynamicThemeEngine.kt` (extracción de paleta desde carátulas, tintado ambiental, guardarraíles WCAG, L1 fallback, L2 `resolveNextTheme` / `DynamicThemeState`, L3 `dynamicThemeFlow` reactivo con retención) |
| Theme editor & preview | `ui/components/ThemeEditorDialog.kt` (personalización HSL, código HEX y verificación WCAG) + `ui/components/ThemeLibraryPreview.kt` (mockup interactivo en vivo de Biblioteca) |

## UI — components / state / theme

| Concern | Archivo |
|---------|---------|
| ViewModel central | `ui/MusicPlayerViewModel.kt` (`rawSongs` identity-slim `stateIn` Eagerly; índices consolidados en pase único `LibraryLookupIndex`; `currentSongId`; `playCurrentLibraryBrowse` / `songsInOrder`; `playAlbum`/`playArtist`/`playGenre`/`playPlaylist` + overloads `play*Next`/`enqueue*` unificando pipeline de colección vía `executeGroupPlayback`; `identifyAlbum(album: Album)`; `songById` para letra; façade playback/download: adjunta `PlaybackRuntime`, expone `ProcessDownloadRuntime.downloads`/conflict/events, `runTrackedDownload` solo adapta `ProcessDownloadRequest`; delega Identify a `identifyCoordinator`; estado cohesivo vía `libraryProjection`, `navigation`, `catalogSearch`, `catalogCollection`, `lbDiscover`, `lbPlaylistDetail`, `cfRecommendations`; no posee jobs ni notificación de descarga) |
| Coordinador Identify Review | `ui/identify/IdentifyReviewCoordinator.kt` (lote identify, cola persistida en `IdentifyReviewStore`, búsqueda de candidatos, filtros artista/álbum/año, aplicación optimista y fan out de álbum conocido; desacoplado de `MusicPlayerViewModel`) |
| Mini player | `ui/components/BottomPlayerBar.kt` (`statusLabel`, Previous/Next/Play; `BottomPlayerProgress` es el único collector del tick 200 ms); wiring `ui/screens/MainScreen.kt`; estado desde los flows de `PlaybackRuntime` reexpuestos por `MusicPlayerViewModel` |
| Active download row | `ui/components/ActiveDownloadRow.kt` |
| Download conflict dialog | `ui/components/DownloadConflictDialog.kt` |
| Song row | `ui/components/SongListItem.kt` (`SongOverflowMenuItems` + Editar letra) |
| Track meta row | `ui/components/TrackMetaRow.kt` (`joinMeta`, `TrackMeta.artistAlbumLabel`, `playingRowColors` / `playingTitleStyle`, `TrackTextColumn`, `TrackMetaRow`) |
| Song queue actions | `ui/components/SongQueueActions.kt` (`SongQueueActions`, `rememberSongQueueActions`) |
| Matched local/remote row | `ui/components/MatchedTrackRow.kt` (`isCurrentPlaying`, `isMatchedTrackPlaying`, `MatchedTrackRow` + overflow opcional); `MatchedTrackLazyColumn.kt` (L1 callbacks / L2 `songActions: SongItemActions`) |
| Remote placeholder row | `ui/components/RemoteTrackPlaceholderRow.kt` |
| Queue list / row | `ui/components/QueueLazyList.kt` (`queueRowKey` / `focusedQueueIndex`, ambos por `queueEntryId`; `DismissibleQueueItemRow` L2 con `SwipeToDismissBox` normalizado y eliminación fiable por ID); `ui/components/QueueItemRow.kt` (`PlayableItemRowContent`, `QueueItemRow` + drag handle); `ui/components/ReorderDragSupport.kt` (`rememberVerticalReorderDrag`) |
| Empty / back header | `ui/components/EmptyListHint.kt` (`actionLabel` / `onAction`); `ui/components/ScreenBackHeader.kt` (`backContentDescription`, default `"Volver"`) |
| Submenu swipe container | `ui/components/SubmenuSwipeBox.kt` (L1 primitivas con flags directos / L2 sobrecarga comprimida con `SubmenuGestureSettings` y `canExecuteAction`, dampening, umbral 72dp, badges visuales, háptica, `SubmenuSwipeAction.icon`; usado en vistas anidadas de biblioteca, playlists y descubrir) |
| Playback chrome | `ui/components/PlaybackUi.kt` (`playPauseVector`, `playbackProgressFraction`, `previewProgressFraction`, `previewFlags`, `PlaybackScrubber`) |
| Control Radio | `ui/components/RadioModeControl.kt` (tap abre menú directo con los 3 modos `RadioMode.entries` KNOWN/NEW/BOTH explicados + Detener; usado por `NowPlayingScreen`) |
| Artwork UI | `ui/components/ArtworkThumbnail.kt` (`rememberArtworkRequest` + `Precision.INEXACT`, `isArtworkCachedInMemory`, `preloadArtwork`, `ArtworkThumbnail`, `ArtworkHero`); `ui/components/ArtworkPicker.kt` (`ArtworkPickerBlock`, `rememberImagePicker`) |
| Song list item & actions | `ui/components/SongListItem.kt` (`SongListItem` L1 callbacks + L2 `SongItemActions`, `SongOptionsMenu` L1/L2), `LibrarySongListHost.kt` (`LibrarySongListActions` `@Immutable` componiendo `songActions` y `albumActions: LibraryAlbumGroupActions`, `SongActionDialogsHost`) |
| Play / shuffle icons | `ui/components/PlayShuffleButtons.kt` (`PlayIconButton`, `ShuffleIconButton`, `PlayShuffleIconPair`, `LabeledPlayShuffleButtons`) |
| Collection headers | `ui/components/CollectionHeader.kt` (`CollectionHeader` base reutilizable, `AlbumHeader` para álbumes L1/L2 con `AlbumHeaderActions`, `PlaylistHeader` para playlists con acciones rápidas de reproducción y menú de edición directa, `PlaylistHeaderActions`, `CircleActionBox` L1, `HeaderActionIcon` L2) |
| Settings switch row | `ui/components/SettingsSwitchRow.kt` (`SettingsSwitchRow`, `SettingsScrollColumn`) |
| Download action widgets | `ui/components/DownloadActionWidgets.kt` (`DownloadStateTrailing`, `downloadStateStatusLabel`, `DownloadSuccessReadyLabel`, `DownloadMissingTracksButton` L1/L2, `AlbumDownloadStateButton`, `MediaCardDownloadSpinner`, `findAlbumDownloadProgress`, `isAlbumDownloading`, `List<ActiveDownload>.findUiDownloadByTrack`, progress/queued/retry/preview primitives) |
| Multi-select bar | `ui/components/MultiSelectActionBar.kt` (`MultiSelectActionBar` L1/L2 con `MultiSelectActions`, `PlaylistAdditionActionBar` con `onSelectAll` / "Todo") |
| Catalog category chips | `ui/components/CatalogCategoryChipsRow.kt` (`CatalogCategoryChipsRow` L2, `CatalogCategoryChip` L1 con icono/label unificados para catálogo y descubrir) |
| Search history UI | `ui/components/SearchHistoryComponents.kt` (`SearchHistoryItemRow` L1, `SearchRecentChipsRow` L2, `SearchHistorySheet` L2 modal con lista completa, vaciar y eliminar individual) |
| Similar preview dialog | `ui/components/SimilarPlaylistPreviewDialog.kt`; state `ui/state/SimilarPlaylistPreviewState.kt` |
| Factories / carga UI | `ui/state/UiStateFlows.kt` (`stateInUi`, `mapToUiState`) y `LoadableUiState.kt` (`LoadPhase`, `LoadableUiState.loading` / `success` / `failure` / `idle`) |
| Proyecciones biblioteca | `ui/state/LibraryProjectionState.kt` (`projectCatalog`, `sameLibraryCatalog`, `catalogLoaded`, `prefsReady`, `songs`/`albums`/`songList` Eagerly, `artists`/`genres`/`recentSongs`/`recentList` WhileSubscribed, haystack solo con query, `LIBRARY_SEARCH_DEBOUNCE_MS`, `playStats`, `buildListModel`; `Dispatchers.Default`; no emite lista nueva si `overlayOpen`) |
| Catálogo agrupado | `ui/state/CatalogUiState.kt` (`CatalogSearchUiState`, `CatalogCollectionUiState`, `CatalogCollectionKind`) |
| Navegación agrupada | `ui/state/UiNavigationState.kt` (`UiNavigationState.toSnapshot` / `fromSnapshot`, `LibraryBrowseStack` transitions); detalle `ui/state/PlaylistDetailNav.kt` |
| Sort helper UI | `ui/components/SortEmphasis.kt` (`sortEmphasisFor`, `sortEmphasisForLastPlayed`); aggregate rows still `SortRelevantInfo.kt` |
| Color picker | `ui/components/ColorPickerDialog.kt` |
| Library list model | `ui/state/LibraryListItem.kt` (`LibraryListModel` `songsVisual`/`segments`/`songsById`/`albumNames`/`keyAt`/`itemAt`/`artworkUriAt`/`uniqueArtworkUrisInRange`/`collapsed`; `LibraryAlbumSegment.songIds`; `AlbumHeader.songIds` / `sortHint`; `SongRow.emphasis` / `artworkUri`); `LibraryUiState.kt` conserva solo `LibraryViewMode` |
| Playlist / nav detail | `ui/state/UiNavigationState.kt` + `PlaylistDetailNav.kt` (`None` / `Local` / `ListenBrainz` / `CfRecommendations`) |
| Discover playback origin | `data/model/DiscoverPlaybackOrigin.kt` (`None` / `ListenBrainz` / `CfRecommendations`, `MatchedLbPlaylist.toDiscoverOrigin`); process state `PlaybackRuntime.discoverPlaybackOrigin`, VM solo lo expone |
| NP song/album actions | `ui/screens/NowPlayingActions.kt` (`NowPlayingActionsMenu` L1/L2, `NowPlayingNavigationActions`, `NowPlayingSongActions` + `from`, `NowPlayingMenuActions`) |
| Identify review state | `ui/state/IdentifyReviewState.kt` (`IdentifyReviewItem`, `IdentifyReviewPhase`, `IdentifyReviewState` `isVisible` / `isApplying` / `pendingCount` / `albumGroups` / `canApplyRemaining` / `canApplySelected` / `identifyHydrationSongIds` / `identifyPersistEcho` / `identifyReviewFromPersisted` / `attachKnownAlbumMatches` / `leftoverIdentifyReview` / `identifySearchDraft` / `withItemSearchChrome` / `mergeIncomingReviewItems` / `Song.forIdentifyReview`); VM `applyPersistedIdentifyQueue` (eco Skip sin Room; extras `getSongsByIds`; hydrate IO fuera de `identifyMutex`) |
| Catalog search coordinator | `ui/state/CatalogSearchCoordinator.kt` (`search` / `searchDebounced` / `submitSearch`, `IdentifySearchFilters`, `CatalogCategory`, StateFlow reactivo) |
| Theme Compose | `ui/theme/Theme.kt`, `ThemePresets.kt`, `ListDensity.kt` (row/artwork/chip density tokens) |

## Domain

| Use case | Archivo | Responsabilidad |
|----------|---------|-----------------|
| `GetLibrarySongsUseCase` | `domain/usecase/GetLibrarySongsUseCase.kt` | filter/sort (`execute` sin copiar `Song`, `searchHaystack` + `IdentifyQueryVariants.searchTokens` / `haystackById`, `compareTo` ignoreCase), `projectCatalog` (`FLAT` ordena pool; `ALBUM_GROUPS` agrupa sin sort global → songs/albums/list + `inheritedArtworkBySongId` en `SongRow.artworkUri`), album groups (`buildListItems` ordena bloques como `extractAlbums` por `albumGroupingKey`; `compareSongsWithinAlbum` / `sortSongsWithinAlbum` / `songsFromListItems` / `songsForAlbum` / `songsInOrder`), `extractAlbums` / `extractArtists` / `extractGenres` (aggregate sort + `sortedAggregates`), `songsForBrowseProjection` / `recentSongs`; wiring reactivo en `ui/state/LibraryProjectionState.kt` |
| `LibraryBrowseFilter` | `ui/state/LibraryBrowseFilter.kt` | chips SONGS/ALBUMS/ARTISTS/GENRES/PLAYLISTS/RECENT |
| `GetDiscoverRecommendationsUseCase` | `domain/usecase/GetDiscoverRecommendationsUseCase.kt` | Generador del feed de recomendaciones para Discover con selección de fuente (`DiscoverSourcePreference`: Ambos/Deezer/ListenBrainz), búsqueda de álbumes vía artistas/releases de LB y Deezer, e intercalado equitativo |
| `GetTopRelatedItemsUseCase` | `domain/usecase/GetTopRelatedItemsUseCase.kt` | Consolidador de los artistas, álbumes y pistas más escuchados combinando estadísticas locales (`playStats` / `lastPlayedAt`) y de ListenBrainz (stats de usuario y listens recientes) con badges de fuente |
| `DownloadAudioTrackUseCase` | `domain/usecase/DownloadAudioTrackUseCase.kt` | wrap download Result (re-lanza `CancellationException`) |
| `MatchListenBrainzTracksUseCase` | `domain/usecase/MatchListenBrainzTracksUseCase.kt` | match LB tracks → local `Song` (delega a `TrackMatchKeys`) |
| `IdentifyPipeline` | `domain/usecase/IdentifyPipeline.kt` | Pipeline de 7 fases para identificar archivos de audio: 1) parse local, 2) acotar artista, 3) búsqueda (álbumes guardados primero), 4) match exacto por ranking, 5) guardar álbum para referencias futuras, 6) enriquecer metadata faltante (`enrichTrackNumber` / `enrichTrackNumberIfMissing`), 7) compuerta de revisión (`evaluateReviewGate`) |
| `CollectionUtils` | `domain/util/CollectionUtils.kt` | Algoritmos reutilizables de colecciones: intercalado equitativo (`interleaveEquitable`) y scoring/ranking de artistas, álbumes y pistas locales (`calculateSongPlayScore`, `scoreLocalArtists` / `calculateTopLocalArtists`, `scoreLocalAlbums` / `recommendLocalAlbums`, `scoreLocalTracks` / `recommendLocalTracks`, `ScoredAlbum`, `ScoredTrack`) |
| `TrackMatchKeys` | `domain/util/TrackMatchKeys.kt` | `normalize` (case + punct + diacríticos) / `containsNormalized` / `matchKey` / `composeKey` / `matchKeyPreNormalized` / `downloadIdFor` / `batchDownloadIdFor` / `downloadIdVariantsFor` (plano + `batch:`, única fuente del anti-duplicado) / `buildLibraryIndex` (= `buildIndex`) / `buildIndex` / `lookupLocalSong` / `matchAgainstLibrary` / `matchMetasAgainstLibrary`; `List<MatchedRemoteTrack>.rematchLocals` en `MatchedRemoteTrack.kt` |
| Album name identity | `domain/util/AlbumNames.kt` | `normalizeAlbumName` / `albumIdentityKey` / `albumNamesMatch` / `stripAlbumEditionDecor` / `isLiveSessionAlbum` / `preferredAlbumDisplayName` (estudio antes que sesión live) / `albumGroupingKey` / `songsByAlbumBucket` / `songsMatchingAlbumBucket` / `pickPersistedAlbumName` (+ `studioKeysByArtist`) / `pickPersistedArtistName` / `libraryAlbumKeysInBucket`; `Album.groupingKey` / `LibraryListItem.AlbumHeader.key` |
| Album track encode | `data/util/AlbumTrackNumbers.kt` | `encodeAlbumTrack` / `albumTrackDisplayNumber` / `albumDiscNumber` / `albumTrackSortKey` / `parseCdTrackNumber` |
| Identify ranking | `domain/util/IdentifyRanking.kt` | `score` / `rank(limit)` / `appendCandidates` / `confidence(ranked, query?)` / `hasSevereConflict` / `titleCollidesWithArtistOrAlbum` / `isGenericIdentifyTitle` / `preferBilingualTitle` / `shouldApplyBilingualTitle` / `catalogSearchText` / `stripTitleNoise` / `isGenericAlbum` / `isPlaceholderArtist` (única fuente: Unknown / YouTube Artist / Enlace Web / track#) / `isPreferredProvider` (incl. ListenBrainz + MusicBrainz) / `cleanIdentityTitle` / `stripLeadingTrackNumber` / `stripArtistFromTitle` / `strongVersionMarkers` / `fieldSimilarity` / `titleFieldSimilarity` / `scriptRunSimilarity` / `compactSimilarity`; `Query.sourceArtist`/`sourceTitle`/`sourceAlbum`/`preferYear`; `IdentifyCatalogQuery.build` |
| Identify query variants | `domain/util/IdentifyQueryVariants.kt` | `expand` / `letterRuns` / `latinHeadDroppingPinyin` / `pinyinTail` / `searchTokens` / `hasHan`; usado por `identifySearchTexts` |
| Identify gaps | `domain/util/IdentifyGaps.kt` | `isWeakIdentityTitle` / `gapApplyFields` / `needsGapIdentify` / `songHasGapsForFields` / `songHasOtherGapsThanArtwork` (título/artista/álbum/portada/pista/año según `applyFields`) |
| Embedded tag fill | `domain/util/EmbeddedTagFill.kt` | `fillSongGapsFromFileTags` / `isPlaceholderGenre` / `isRemoteCatalogArtwork` |
| Identify apply diffs | `domain/util/IdentifyApplyChanges.kt` | `identifyApplyChanges` / `formatIdentifyApplyChanges` / `IdentifyFieldChange`; fields `IdentifyApplyField` / `IdentifyApplyFields.ALL`/`NONE` / `isEnabled` / `withField` |
| Filename identity hints | `domain/util/FilenameIdentityHints.kt` | `parseFilenameMetadataHints` / `splitUsingKnownArtists` / `resolveWeakIdentityHints` (delega `IdentifyRanking.isPlaceholderArtist`) / `mergeIdentityHints` / `tidyFilenamePhrase` / `identifySearchTexts` / `looksLikeDiscTrackRip` / `isTrackNumberLabel` / `stripLeadingTitleJunk` / `splitArtistTitleDash` / `TRACK_SPACE_TITLE` y sufijos de pista `hintsFromTitleSuffixTrack` |
| `MetadataSplitter` | `domain/util/MetadataSplitter.kt` | `splitArtists` / `splitGenres` / `normalizeKey` / `buildCandidateArtists`; **identity keys**: `artistIdentityKey` (fold diacritics + connectors + dynamic mixed-script Latin extraction) / `genreIdentityKey` (fold + lemmatize + canonical token order); **display name selectors**: `preferredArtistDisplayName` (original-script priority, diacritics bonus, Title Case over lower/ALL-CAPS) / `preferredGenreDisplayName` (space-separated, singular, Title Case); separa colaboraciones y multi-artistas mediante sintaxis de tags y coincidencia dinámica con artistas conocidos (`knownArtists`), preservando bandas sin listas estáticas; descarta cabeceras compuestas redundantes en campos `;`-delimitados |
| Identify album groups | `domain/util/IdentifyAlbumGroups.kt` | `IdentifyAlbumGroup` / `clusterIdentifyAlbumGroups` / `clusterIdentifyAlbumGroupsFromSources` / `IdentifyAlbumGroupSource` / `albumGroupKey` |
| Identify album batch resolver | `domain/util/IdentifyAlbumBatchResolver.kt` | `IdentifyAlbumBatchCandidate` / `AlbumBatchSongPartition` / `AlbumTrackMatchResult` / `findAlbumBatchCandidates` / `findNextAlbumBatchCandidate` / `findNextAlbumBatchCandidateForSong` / `isEligibleForAlbumBatchIdentify` / `partitionAlbumBatchSongs` / `buildKnownAlbumProposal` / `applyKnownAlbumMatches` / `matchAndApplyKnownAlbumTracks` |
| Natural text ordering | `domain/util/NaturalTextOrder.kt` | `comparator` / `sectionDescriptor` / `extractRomanizedOrParsed` / `transliterateToLatin` (A-Z → 0-9 → *) |
| Identify known albums | `domain/util/IdentifyKnownAlbums.kt` | `KnownAlbumTracks` / `knownAlbumsFromLibrary` / `mergeKnownAlbumTracks` / `matchSongToKnownAlbum` / `assignUniqueKnownAlbumMatches` / `promoteKnownAlbumMatches` / `toIdentifyCandidate`; UI `attachKnownAlbumMatches` |
| `ImportListenBrainzPlaylistUseCase` | `domain/usecase/ImportListenBrainzPlaylistUseCase.kt` | create Room playlist: matched + `PlaylistPendingTrack` metadata |
| `BuildSimilarPlaylistPreviewUseCase` | `domain/usecase/BuildSimilarPlaylistPreviewUseCase.kt` | multi-seed `RadioEngine.suggestFromSeeds` + co-playlist union; `createPlaylistFromPlayables` (Local + pending Remote) |
| `FetchAndMatchCfRecommendationsUseCase` | `domain/usecase/FetchAndMatchCfRecommendationsUseCase.kt` | CF mbids → metadata → Local/Remote |
| `BrowseLocalLibraryUseCase` | `domain/usecase/BrowseLocalLibraryUseCase.kt` | snapshot local para Auto/AVRCP, álbum/artista/search y paginación |
| `createBestiaPopRadioEngine` | `domain/radio/BestiaPopRadioEngineFactory.kt` | arma el único grafo de radio compartido por runtime y preview UI |
| `RadioEngine` | `domain/radio/RadioEngine.kt` | orquesta KNOWN / NEW / BOTH; `suggest` / `suggestFromSeeds`; fill LB→CF→`SimilarTracksProvider`; `interleaveEquitable` / `roundRobinMerge`; `RadioSuggestResult` |
| `SimilarTracksProvider` | `domain/radio/SimilarTracksProvider.kt` | contrato de fill remoto (Deezer, futuros) |
| `LocalMetadataRadio` | `domain/radio/LocalMetadataRadio.kt` | score biblioteca (artista/género/año/álbum/co-playlist) |
| `ListenBrainzRadio` | `domain/radio/ListenBrainzRadio.kt` | lb-radio → Local/Remote |
| `CfRecommendationsRadio` | `domain/radio/CfRecommendationsRadio.kt` | CF pool cache → Local/Remote (fill Radio NEW/BOTH) |
| `DeezerSimilarRadio` | `domain/radio/DeezerSimilarRadio.kt` | Deezer radio/related + iTunes same-artist fill → Remote |
| `RadioMode` | `domain/radio/RadioMode.kt` | `KNOWN` / `NEW` / `BOTH` |
| Puerto | `domain/repository/IMusicRepository.kt` | contrato repositorio (`allSongsFlow`, `songPlayStatsFlow`, `getAllSongsSync`, `getSongsByIds`, `getSongById`, `getPlaylistIdsForSong`, `getCoPlaylistSongIds`, `touchSongLastPlayed`, `getSongsForArtist`, `getSongsForGenre`, `getAlbumsForArtist`, `LibraryScanProgress`, `pruneUnplayableCorruptSongs`, `proposeSongIdentity` (+ `listenBrainzToken`, `IdentifySearchFilters`, paginación), `applySongIdentity` / `applySongIdentities` (`IdentifyApplyRequest`), `identifySongMetadata`, `syncTagsToFiles`, `saveUploadedSong(Song)`, `setAlbumArtwork(albumKey, artworkUri)` solo-portada vs `updateAlbumMetadataPropagateToSongs`) |

## Data

| Concern | Archivo |
|---------|---------|
| Repo impl | `data/repository/MusicRepository.kt` (`scanMediaStore` / `resyncAppManagedMusic` / `scanFolderUri` → `List<Song>` via `persistInsertedSongs` / `getSongsByUris`; walk filesystem si el tree URI mapea y `File.listFiles` responde, si no DocumentFile; `indexSourcesParallel` `SCAN_FILE_PARALLEL=4` + `AlbumArtworkCache`; `pruneUnplayableCorruptSongs` purga filas zombie `durationMs <= 0` sin identidad válida; `isRealMusicTrack` descarta `durationMs <= 0` con tags genéricos; `syncSongsRelations` sincroniza entidades relacionales 3FN (`artists`, `genres`, referencias cruzadas de canción y álbum); `resolveTrackNumberFallback` deducción vía catálogo oficial (Deezer/iTunes); `getSongsForArtist`, `getSongsForGenre`, `getAlbumsForArtist`; `proposeSongIdentity` (+ `listenBrainzToken`, `IdentifySearchFilters` / `IdentifyCatalogQuery` / `catalogSearchText` / `identifySearchTexts`, `needsGapIdentify`, soft `persistWeakIdentityCleanup` solo título basura, match global `matchKnownAlbumFromLibrary` antes de catálogo, `fetchIdentifyCatalogTracks` LB→MusicBrainz→Deezer/iTunes, append page, `searchIdentifyFallbacks` si no HIGH), `loadKnownAlbumTracks` (Room identidad + `searchAlbums`/`fetchAlbumTracks`, cache de sesión), `applySongIdentity` / `applySongIdentities` (`updateSongIdentity` en `withTransaction`; tags después; `preferBilingualTitle` si el catálogo aporta otro script), `identifySongMetadata` (HIGH sin título salvo bilingüe), `maybeWriteTags` / `syncTagsToFiles`, `migrateCanonicalAudioUris` / `migrateDateAddedFromDevice` / `migrateEmbeddedFileTags` (batch `@Transaction` sobre `getIdentitySongs`), `enhanceSongMetadataAndLyricsBatch`, `getSongById` / `getSongsByIds`, snapshot `getIdentitySongs`, `getAllSongPathRefs`, caches en memoria `identityLibrarySongs`, `libraryArtistsCached`, `libraryKnownAlbumsCached` para evitar clonados y re-clusters) |
| Identidad de track | `data/model/TrackIdentity.kt` (`TrackMeta`, `TrackIdentity`, `mergePreferring`, `youtubeSearchQuery`, `toCatalogTrack`, `toListenBrainzCatalogTrack`, `preferMetaFrom`, `Song.toIdentity`, `Song.withIdentity`, `OnlineCatalogTrack.withIdentity`, `DEFAULT_CATALOG_USER_AGENT`); JSON shared `data/util/TrackIdentityJson.kt` (`putInto` / `decode`, compat `artworkUrl`→`artworkUri`) |
| Modelos dominio UI | `data/model/Models.kt`: `OnlineCatalogTrack` / candidates/catalog; `ActiveDownload` (`TrackMeta`, transitions `asDownloading`/`asConflict`/`asSuccess`/`asError`, `restoredConflictPolicy`); `DownloadLane` + `ActiveDownloadSource.lane`; `CandidateDownloadState.isInFlight/isFailed`; único `DownloadPlaylistDestination` + `resolveDownloadPlaylistDestinations`; playlists/WiFi/identify models. `Song.kt` (`@Immutable`) / `SongPathRef.kt` (`uriString`, `folderPath`) / `AlbumOverride.kt` / `SongPlayStat.kt` = Room planos |
| Cola Local/Remote | `data/model/PlayableItem.kt` (`PlayableItem` : `TrackMeta`; Local y Remote llevan `queueEntryId`; `withFreshQueueEntryIds`, `indexOfQueueEntry` / `indexOfRemoteSlot`, `matchesSong` / `matchesItem`, `ResolvedStream`, `Song.toPlayable`, `remoteFrom` identity + args, `fromLibraryOrRemote(identity)` / args, `Remote.toOnlineCatalogTrack` / `withIdentity`) |
| Room DB | `data/db/AppDatabase.kt` (v15; `MIGRATION_9_10` `song_play_stats`, `MIGRATION_10_11` índices individuales, `MIGRATION_11_12` title, `MIGRATION_12_13` compuesto `artist_album`, `MIGRATION_13_14` artwork uri index, `MIGRATION_14_15` 3FN `artists`, `genres`, referencias cruzadas) |
| Entidades relacionales (3FN) | `data/db/ArtistEntity.kt` (`ArtistEntity`), `data/db/GenreEntity.kt` (`GenreEntity`), `data/db/MusicCrossRefEntities.kt` (`SongArtistCrossRef`, `SongGenreCrossRef`, `AlbumArtistCrossRef`, `AlbumGenreCrossRef`) |
| DAO | `data/db/MusicDao.kt` (`getAllSongsFlow` identity-slim sin `lyrics` ni `ORDER BY`, `lastPlayedAt=0`; `getPlaylistSongsOrderedFlow` / `getPlaylistSongsOrdered` vía `PLAYLIST_SONG_SELECT` sin `lyrics`; `getSongsForAlbum` vía `IDENTITY_SONG_SELECT` sin `lyrics`; `getAllSongPathRefs` proyección ligera; `getPlayStatsFlow` / `getPlayStat` / `deletePlayStatsForSongs`; `getSongById` / `getAllSongs` fila completa; `insertSong` / `insertSongs` = `OnConflictStrategy.IGNORE`, `getSongByUri` / `getSongsByUris`, `getIdentitySongs` / `getIdentitySongsByIds`, `getSongsWithNonPositiveDuration`, `updateSongIdentity`, `deletePlaylistRefsForSongs`, `getPlaylistIdsForSong`, `getCoPlaylistSongIds`, `updateLastPlayedAt` → stats; queries relacionales de artistas, géneros y referencias cruzadas; `updateTrackNumber`) |
| Song entity + MediaStore | `data/model/Song.kt` (`lastPlayedAt` columna legacy); `data/model/SongPlayStat.kt`; `data/db/MediaStoreSongMapper.kt` (`Cursor.toSong`) |
| Album overrides | `data/model/AlbumOverride.kt`; DAO `getAllAlbumOverridesFlow` / `upsertAlbumOverride`; repo `persistOverride` (sanitize art/name), `setAlbumArtwork` (solo portada), `insertOrUpdateByUri` (alta preservando id) |
| Album merge | `IMusicRepository.mergeAlbumInto` → `MusicRepository.mergeAlbumInto` + `MusicDao.updateSongsAlbumMetadata` / `getSongsForAlbum` |
| Playlist entities | `data/db/PlaylistEntities.kt` (`PlaylistPendingTrackEntity` plano, columnas `releaseName` + `trackNumber`); mapper `toPendingTrack` / `toEntity` en `MusicRepository.kt` (`album`/`trackNumber` ↔ columnas) |
| Catálogo / lyrics / covers web | `data/network/MetadataFetcher.kt` (`JSONObject.toDeezerTrackIdentity` / `toItunesTrackIdentity`, `parseDeezerTrackArray` → `List<TrackIdentity>`, `parseDeezerSearchTracks`, `parseItunesSongResults`, `parseCatalogGenres`, `listGenres`, `fetchChartTracks`, `searchTracksByGenre`, `fetchFullTrackMetadata` → `TrackIdentity?`, `searchIdentifyFallbacks` (iTunes US+JP+TW, MusicBrainz, YouTube), `toCatalogCandidate`, `searchDeezerArtist`, `resolveDeezerArtistId`, `fetchDeezerArtistRadio`, `fetchDeezerRelatedArtistIds`, `fetchDeezerArtistTop`, `fetchItunesArtistSongs`) |
| MusicBrainz identify | `data/network/MusicBrainzClient.kt` (`searchRecordings`, `parseMusicBrainzRecordingSearch`, `luceneRecordingQuery`; mutex 1 req/s; timeout no duration-retry; 3 fallos → skip 5 min; CAA `coverartarchive.org/release/{mbid}/front-500`) |
| YouTube search + stream | `data/network/YouTubeExtractor.kt` (`YouTubeStreamResult(identity, videoId, audioUrl, userAgent)` : `TrackMeta` + invoke L2, `extractYouTubeId`, `searchYouTube`, `parseSearchContents` con parsing de duración por runs/overlays y detección de transmisiones en vivo, scoring multiseñal `audioPreferenceScore` y `rankByAudioPreference` comprimidos con `expected: TrackMeta?` + sobrecargas L1 primitivas, penalización a live streams `-300` y snippets/shorts `-200`, bonificación por álbum `+25`, bonificación/penalización por duración, coincidencia de palabras clave de título, `resolveYouTubeQueryOrId` filtrando ISRCs, `AUDIO_CLIENTS` TVHTML5 5.x / VISIONOS / ANDROID / ANDROID_MUSIC / TVHTML5 7.x; no ANDROID_VR) |
| HTTP Range googlevideo | `data/network/GoogleVideoRange.kt` (`remainingLength` / `httpRangeHeader` / `nextChunkRange`); playback `MusicService.boundGoogleVideoRequest` / `googleVideoBoundedLength`; download `MusicRepository.downloadAndSaveOnlineTrack` |
| Caché multimedia unificado | `data/stream/BestiaPopMediaCache.kt` (`BestiaPopMediaCache`: `SimpleCache` process-scoped singleton 150MB LRU, `cacheKey` `yt:videoId` compartido entre ExoPlayer `CacheDataSource` y `MusicRepository` download chunk/prefix transfer) |
| Stream resolve + cache TTL | `data/stream/StreamResolver.kt` (`resolveForPlayback(item, maxCachedAgeMs)` limita edad de playback; `resolve` compat; `resolveQuery(forceRefresh, expected: TrackMeta?, fallbackQuery)` download; `MusicRepository.resolveTrackStreamForDownload` pasando `expected = track.identity`; `invalidate(item)` borra claves `id:` + `q:`; `withKeyLock` serializa por query con reserva ref-counted antes del lock; `pruneKeyLocksLocked` nunca poda entregados y `pruneLocked` acota cache). Instancia playback única en `BestiaPopApplication.musicRepository`; `PlaybackRuntime` usa 60s |
| Clientes HTTP compartidos | `data/network/HttpClients.kt` (`api` con `callTimeout`, `transfer` sin cap para bytes). Todos los módulos derivan con `newBuilder()`; antes había seis clientes independientes y ninguno con `callTimeout` |
| Playable factories | `data/model/PlayableItem.kt` (`remoteFrom(identity)` / `remoteFrom(artist, title, …)`, `fromLibraryOrRemote(identity)` / args, `Remote.toOnlineCatalogTrack`, `Remote.withIdentity`) |
| Theme DataStore | `data/preferences/ThemePreferencesRepository.kt` (persistencia y mirror SharedPreferences de presets, custom y dynamic colors + `lastDynamicArtworkUri`; compresión semántica `ColorSchemeData`) |
| Library initial scan + UI prefs | `data/preferences/LibraryPreferencesRepository.kt` (`isInitialScanCompleted`, `displaySettingsFlow`, `navSnapshotFlow`, `submenuGestureSettingsFlow`, `setSortOptionName` / `setSortDirectionName` / `setViewModeName` / `setNavSnapshot` / `setSubmenuSwipeBackEnabled` / `setSubmenuSwipeLeftAction`, one-shots `canonical_audio_uris_migrated` / `device_date_added_migrated` / `embedded_file_tags_migrated_v4`); codec `LibraryUiPreferences.kt` (`LibraryUiPreferencesCodec`, `LibraryStackLookups.fromSongs`, `UiNavSnapshot.browseFilterName` + legacy `library_tab`, `LibraryDisplaySettings` sort+direction, `SubmenuSwipeAction`, `SubmenuGestureSettings`, `sanitizeBrowseFilterName` / `sanitizeSortDirectionName`) |
| Playback / sonido + modos | `data/preferences/PlaybackPreferencesRepository.kt` (`PlaybackSettings`, `PlaybackModeRestore`, `PlaybackModeClear.afterManualPlay` / `afterSkip` / `afterRadioStart`, `parseRepeatModeName`, `MAX_VOLUME_BOOST_GAIN_MB`, `stereoLeftGain` / `stereoRightGain`, `rememberShuffleOnLaunch` / `rememberRepeatOnLaunch` / `autoplayOnLaunch`, `lastShuffleEnabled` / `lastRepeatMode`, `openNowPlayingOnPlay`, `crossfadeEnabled`, `crossfadeDurationSeconds`, clear-on-play/skip, `oemScreenOffCleanupHintDismissed`); writes 1-key vía `DataStorePrefs.kt` `put`; readiness process-scoped vía `PlaybackRuntime.awaitPlaybackSettings` |
| Lyrics preferences | `data/preferences/LyricsPreferencesRepository.kt` (`LyricsSettings`, `JapanesePhoneticMode`; persistencia de `phoneticGuideEnabled`, `japanesePhoneticMode`, `askBeforeGoogleTranslate`) |
| Active downloads persist | `data/preferences/ActiveDownloadsStore.kt` (`ActiveDownloadCodec`: request, todos los `DownloadPlaylistDestination`, `lookupIdentity`, `batchId`, overwrite/SaveAs, `downloadStarted`/`storageCommitted`, marker `interrupted`, compat legacy; `activeDownloadBadgeCount`) detrás del único writer `ProcessDownloadCoordinator` |
| Identify review persist | `data/preferences/IdentifyReviewStore.kt` (`IdentifyReviewCodec`, `PersistedIdentifyReviewQueue`, `appendProposals` / `mergeUiRemaining` / `removeSongIds` / `pendingSongIds`; sin `audioUrl` CDN) |
| Identify work persist | `data/preferences/IdentifyWorkStore.kt` (`IdentifyWorkCodec`, `IdentifyWorkSnapshot` remaining IDs + `fillGapsOnlySongIds`) |
| Last-played + cola persistida | `data/preferences/PlaybackSessionStore.kt` (`LastPlayedCodec`, `QueueSnapshotCodec`, `PlaybackHydration.hydrateQueue` — cola Remote-only aun con biblioteca vacía + ids frescos—, `saveSession`, `LastPlayedSnapshot(identity)`, `QueueSnapshot` + `shufflePlayOrder`, `PersistedQueueItem`, `HydratedQueue`) |
| Wrap / trim / shuffle índices | `data/playback/PlaybackQueueOrder.kt` (`rotateToStart`, `trimHistory`, `shufflePlayOrder`, `reshufflePlayOrder`, `reshuffleItemsAvoidingFirst`, `reshuffleItemsAvoidingKey`, `insertAfterCurrent`, `appendToPlayOrder`, `removeFromPlayOrder`, `moveInPlayOrder`, `remapPlayOrder`, `MAX_QUEUE_HISTORY`) |
| Slots / snapshot shuffle | `data/playback/PlaybackQueueSlots.kt` (`capturePreShuffleOrder`, `restorePreShuffleOrder`, `projectSnapshot`; identidad por `queueEntryId`) |
| Fallback circular | `data/playback/PlaybackFallbackPlanner.kt` (`PlaybackFallbackPlanner.circularPlan`, `PlaybackFallbackStep.ReadyLocal` / `ResolveRemote`) |
| Intención latest-wins | `data/playback/PlaybackSelectionIntentGate.kt` (`PlaybackSelectionIntentGate.beginRemoteSelection` / `isCurrent` / `invalidate` / `onLocalSelected`) |
| Cambio de track | `data/playback/PlaybackTrackChangePolicy.kt` (`PlaybackChangeHint`, `PlaybackTrackChange`, `PlaybackTrackChangePolicy.resolve` / `sameIdentity`) |
| ListenBrainz API | `data/network/ListenBrainzClient.kt` (`submitListens`, createdfor, playlist, `lookupRecordingMetadata`, `fetchLbRadioArtist`, `fetchRecordingMetadata`, `fetchCfRecordingRecommendations`, `fetchCfRecordingsWithMetadata`, `fetchUserTopArtists`, `fetchUserTopReleases`, `fetchUserTopRecordings`, `fetchUserRecentListens`) |
| LB models + sync | `data/listenbrainz/LbPlaylistModels.kt`, `data/listenbrainz/LbUserStatsModels.kt` (`LbUserStatArtist`, `LbUserStatRelease`, `LbUserStatRecording`), `data/listenbrainz/MatchedRemoteTrack.kt` (mappers/match/rematch), `data/listenbrainz/LbRadioModels.kt`, `data/listenbrainz/CfRecommendationModels.kt`, `data/listenbrainz/ListenTracker.kt` (`onTrackChanged`), `data/listenbrainz/ListenSyncCoordinator.kt` (`requestSync`) |
| Mensajes y copys | `data/model/PlaylistMessages.kt` (copy central de playlists: crear, borrar, añadir, feedback), `data/model/DownloadProgress.kt` (`DownloadMessages`: copy central de descargas, badges, estados y conteos) |
| Guardar al escuchar policy | `data/listenbrainz/SaveWhileListeningPolicy.kt` (`SaveWhileListeningEvent`; `SaveWhileListeningPolicy.shouldSave`) |
| Connectivity | `data/network/ConnectivityObserver.kt` (`isCurrentlyOnline`, `isMetered`, `networkTypeLabel`; override internal `configureForTest` / `resetTestOverrides` para fronteras E2E herméticas) |
| Background execution & estabilidad | `data/system/BackgroundExecutionStatus.kt` (`BackgroundExecutionProbe.current`; `openApplicationDetails` / `applicationDetailsIntent`; `restrictionGuidance` / `backgroundRestrictionGuidance`; `oemScreenOffCleanupIntent` / `openOemScreenOffCleanupSettings` → `unisoc.intent.action.POWER_BACKGROUND_CLEAN` + `lock_screen_battery_save`; `oemScreenOffCleanupEnabledFromSettings` / `parseOemScreenOffCleanupEnabled` / `oemScreenOffCleanupActive`; `isRunAnyInBackgroundBlocked`; señales `backgroundRestricted` / `runAnyInBackgroundIgnored` / `ignoringBatteryOptimizations`; `blocksBackgroundPlayback` = `isBackgroundRestricted` ∧ AppOps IGNORE); `data/preferences/TelemetryPreferencesRepository.kt` (`telemetryEnabledFlow`, `setTelemetryEnabled`, `isTelemetryEnabledSync`); `data/system/SystemStabilityMonitor.kt` (`checkHistoricalExitReasons` post-mortem LMK/recursos/ANR con opt-in sincrónico e idempotencia de reporte; `recordMemoryTrim` en niveles críticos; `recordLowMemory`) |
| GitHub Releases update | `data/update/GitHubReleaseParser.kt` (`parseReleases` / `parseRelease` / `parseVersionCode` / `stripVersionCodeLine`); `GitHubUpdateClient.fetchReleases`; `data/update/AppReleaseSelection.kt` (`from`, `updateTarget`); `data/update/ApkUpdateInstaller.kt` (`ApkUpdateDownloader.download`: `.part`, HTTP/length, `ApkValidator` / `PackageManagerApkValidator`, move atómico + cleanup; `ApkUpdateInstaller` FileProvider/instalador); `AppUpdateCheckStore` (`lastCheckAtMs`, `cachedNotes` / `setCachedNotes`); `data/update/AppRelease.kt` (`AppRelease`, `GitHubReleaseUrls.repoUrl` / `latestPageUrl` / `apiReleasesUrl`) |
| Pending listens Room | `data/db/PendingListenEntity.kt`, `PendingListenDao.kt` |
| Storage / transfer helpers | `data/util/MusicFileStore.kt` (`readableFile` / `writableFile`), `AudioPersistRef.kt`, `StorageUtils.kt` (PendingWrite/publish/list/delete), `TransferIo.kt` (`copyTransferToFile`: L1 compartido música+APK; políticas quedan arriba), `SongPathNormalizer.kt` (`toAbsolutePath` también `/tree/` root), `UploadNameSanitizer.kt` (`sanitize` preserva espacios y filtra solo caracteres ilegales FS / `asciiLegacy` / `matchingBasenames`), `AudioFileMetadata.fromPath` (jaudiotagger + MMR fallback, year/lyrics, `AlbumArtworkCache` opcional), `AudioTagReader.kt`, `AudioTagWriter.kt`, `SyncedLyrics.kt` (`parse` / `format` / `plainText` / `realignByText` / `stamp` / `currentLineIndex`), `LyricsPhoneticProcessor.kt` (detección de alfabetos no latinos, Rōmaji, Hiragana, katakanaToHiragana, formatPhoneticLine), `DisplayLyricLine.kt` |
| Lyrics translation & network | `data/network/LyricsTranslationService.kt` (`fetchCommunityTranslation` Musixmatch, `translateWithGoogle`, `fetchRomanization`, `LyricsTranslationResult`, `LyricsTranslationSource`) |
| Audio persist refs / sync | `data/util/AudioPersistRef.kt` (`resolve`, `toPersistKey`, `normalizeRelative`, `ensureCanonicalDirectory`) |
| Metadata fetcher | `data/network/MetadataFetcher.kt` (iTunes/Deezer/MusicBrainz; chart, search, album, radio, similar) |
| YouTube extractor | `data/network/YouTubeExtractor.kt` (`extractDirectAudioStreamUrl`, retry, CDN format preference) |

## Services

| Servicio | Archivo |
|----------|---------|
| Playback runtime process-scoped | `service/PlaybackRuntime.kt` (`playPlayableCollection`; `seekToAndPlay`; `connect` arma el connector sin `startService`; `onPlaybackStartedFromService` reconexión externa; `onTaskRemovedNotEngaged`; `shouldRetainController` demand-driven sin retener por cola idle; `systemResumptionMetadataSnapshot` puro / `restoreSystemPlaybackSnapshot` mutante bajo mutex compartido; `stageExternalPlayableCollection` para browse; `removeFromQueue(index)` y L2 `removeFromQueue(queueEntryId)`; reconnect/generaciones/fallback/prefetch; `syncChangedTimelineItems` para metadatos/portadas; wrap limpio en shuffle repeat-all; writer serial; ticker/tracker/autosave/radio; `loadSongById` / `loadSongsByIds` / `songsForHydration` / `hydrateCurrentSongLyrics` / `Song.keepLyricsIfIncomingSlim` para NP; `refreshLocalQueueMetadata` filtrado por cola) |
| Frontera Media3 portable | `service/PlaybackMediaItemCodec.kt` (`encode` / `decode`, payload v1 Local/Remote; CDN solo URI); `service/PlaybackMediaMetadata.kt` (builder común desde `TrackMeta`); `service/StreamPlaybackTag.kt` (UA en extras) |
| Playback Media3 + notif/lifecycle FGS | `service/MusicService.kt` (`DefaultRenderersFactory` con `setEnableDecoderFallback(true)` y selector de códecs que prioriza decodificadores audio hardware del SoC sobre fallback software aislando Unisoc; `DefaultLoadControl` con buffer determinista; `onUpdateNotificationAsync`; `maybeNotifyBackgroundRestriction`; `shouldResumeAfterStickyRestart`; `onPlaybackStartedFromService` re-hook; `onTaskRemoved` con detención/limpieza cuando !isPlaybackEngaged; `requestResumeAfterServiceRestart` en runtime; shuffle; HTTP googlevideo; `playbackWakeMode` restringido a streaming remoto; crossfade loop con targetVolume en seek/pausa) + `service/PlaybackCrossfade.kt` (`calculateCrossfadeVolume` puro con bypass seguro en pistas <1.5s) + `service/RefreshingMediaNotificationProvider.kt` (artwork async vuelve por el hook para conservar Remote IDLE); `service/library/BestiaPopMediaLibraryCallback.kt` (`onConnectAsync` + `untrustedTransportPlayerCommands` para transporte-only de launchers/widgets; resumption/browse/search/set items); `PlaybackSystemResumption.kt`; `PlaybackServiceLifetimePolicy` |
| Browse Auto/AVRCP | `service/library/MediaLibraryIds.kt`, `MediaLibraryBrowseMapper.kt`, `MediaLibraryBrowseProvider.kt` (sources versionadas con `stateIn`; snapshot lazy cacheado bajo mutex y construido en Default), `BestiaPopMediaLibraryCallback.kt`; playlist ordenada `MusicDao.getPlaylistSongsOrdered`; manifest `automotive_app_desc.xml` |
| Registry de descargas process-scoped | `service/ProcessDownloadCoordinator.kt` (`execute`, claim aliases, permit 3, `DownloadPlaylistDestination`, persist, cancel; `updateProgress` coalesced + writes estructurales inmediatos; `interruptNow(lane)` / `dismissRunning(lane)`). `BestiaPopApplication` instala callback add/remove pending |
| Runtime de descargas process-scoped | `service/ProcessDownloadRuntime.kt` (`ProcessDownloadRequest`, private `DownloadExecutionContext`, submit/retry/resume/conflict, durable lease/reconcile; façade UI `isRunning` / `findClaimedDownload` / `attachPlaylistDestination` / `upsertRow`; lane settle/stop) |
| Runtime de identify process-scoped | `service/ProcessIdentifyRuntime.kt` (`submit(..., fillGapsOnly)` / `resumeInterrupted` / `cancelUser` / `settle`; `IDENTIFY_PARALLEL=2` workers, throttling a 1 + delays dinámicos durante reproducción activa y espera de streaming online; `PERSIST_EVERY=8`; `getSongById` / `getSongsByIds`; HIGH auto `fields.copy(title = false)` o `gapApplyFields` si `fillGapsOnlySongIds`; fan-out scoped `loadScopedAlbumTracks` tras HIGH; pausa offline; writes de tags pospuestos para la pista activa en `MusicRepository`; progress IDENTIFY; review bufferizada; eventos Completed/AlreadyQueued) |
| Lifetime identify Android | `IdentifyExecutionLauncher.kt`; `IdentifyJobService` UIDT; `IdentifyForegroundService` FGS dataSync; `IdentifyNotificationHelper` (`TAB_IDENTIFY_REVIEW`) |
| Guardar al escuchar process-scoped | `service/ProcessSaveWhileListeningCoordinator.kt` (`save` / `dismiss`; usa el registry compartido, éxito inmediato si ya existe, `SaveWhileListeningDownloadResult.InFlight` neutral si el claim pertenece a otro owner) |
| Stereo balance (PCM) | `service/StereoBalanceAudioProcessor.kt` (`queueInput`) + `MusicService.applyStereoBalance`; seam instrumentado debug `MusicServiceSettingsProbe.observe` (`MusicServiceSettingsProbe.kt`, inerte sin observer) |
| Volume boost (LoudnessEnhancer) | `MusicService.applyBoost` + `PlaybackPreferencesRepository`; `MainActivity.dispatchKeyEvent` + `MusicPlayerViewModel.handleVolumeUp` / `handleVolumeDown` + `ui/components/VolumeBoostHud.kt`; el mismo `MusicServiceSettingsProbe` observa gains/target mB sin capturar audio |
| Ktor WiFi server | `service/WebServerService.kt` (`WifiSyncHttpBoundary`; `WifiLock` + `WakeLock` lifecycle; `onTimeout` → `markWifiTransfersTimedOut` + stop FGS; state/transfers; `/existing-files`); identify post-upload en VM |
| Download progress notif | `service/DownloadNotificationHelper.kt` (`EXTRA_OPEN_TAB` / `TAB_DOWNLOADS`) |

## Tests de referencia

| Tipo | Archivo |
|------|---------|
| Library list items | `app/src/test/.../GetLibrarySongsUseCaseListItemsTest.kt` |
| Album track encode / catalog parse | `app/src/test/.../AlbumTrackNumbersTest.kt`, `CatalogTrackNumberParseTest.kt` |
| YouTube extraction | `app/src/test/.../YouTubeExtractionIntegrationTest.kt` |
| YouTube audio preference | `app/src/test/.../YouTubeAudioPreferenceTest.kt` |
| StreamResolver cache/TTL | `app/src/test/.../StreamResolverTest.kt` (incl. reserva ref-counted bajo poda concurrente antes de adquirir mutex) |
| Playable mediaId / queue slot | `app/src/test/.../PlayableItemRemoteMediaIdTest.kt` (`queueEntryId` Local+Remote, duplicados, `withFreshQueueEntryIds`, `indexOfQueueEntry` / `indexOfRemoteSlot`) |
| Slots / fallback / latest intent | `app/src/test/.../PlaybackQueueSlotsTest.kt`, `PlaybackFallbackPlannerTest.kt`, `PlaybackSelectionIntentGateTest.kt` |
| Queue UI key / foco | `app/src/test/.../ui/components/QueueUiIdentityTest.kt` (duplicados exactos + reorder por `queueEntryId`) |
| Tracker / guardar al escuchar | `app/src/test/.../PlaybackTrackChangePolicyTest.kt`, `ListenTrackerTest.kt`, `SaveWhileListeningPolicyTest.kt` |
| Continuidad process/runtime | `app/src/test/.../service/PlaybackRuntimeContinuityTest.kt` (facade inválido tras disconnect + restore intención/slot/posición; generaciones contra completions transition/prefetch/recovery; fallback circular >5 + anti-cascada; seek pausado serial; readiness Playback/LB; callbacks externos repeat/shuffle/timeline; pausa resolve/buffering, display-only, persistencia, lease/ticker, detach UI/tracker/save/radio; NP hidrata lyrics con `loadSongById`), `PlaybackServiceLifetimePolicyTest.kt` (task removed + placeholder IDLE) |
| Simulación concurrencia y estrés total | `app/src/test/.../service/FullServiceStressSimulationTest.kt` (ejecución simultánea de PlaybackRuntime con cambio rápido de colecciones/álbumes/artistas/playlists/remotos, ProcessIdentifyRuntime masivo, ProcessDownloadRuntime paralelo con chunk/progress, transiciones foreground/background, y simulación de batería desconectada/discharging con zero wake locks para pistas locales) |
| Notificación playback / controllers externos | `PlaybackNotificationContractTest.kt` (foreground policy Remote IDLE + sticky resume + `WAKE_MODE_NONE` local); `BackgroundExecutionStatusTest.kt` (`blocksBackgroundPlayback` exige `isBackgroundRestricted` + AppOps IGNORE; `SystemStabilityMonitorTest.kt` (mapeo y filtrado de salidas LMK/recursos/ANR, niveles críticos trimMemory, jerarquía de excepciones); `BestiaPopMediaLibraryCallbackTest.kt` (policy transporte-only no confiable); `LocalPlaybackServiceFunctionalTest.kt` (provider real, acciones/FGS); `LockedScreenPlaybackFunctionalTest.kt` (host-only sin appops) |
| Codec MediaItem | `app/src/test/.../service/PlaybackMediaItemCodecTest.kt` (round-trip portable Local/Remote; CDN omitida) |
| HTTP Range googlevideo | `app/src/test/.../data/network/GoogleVideoRangeTest.kt`; `MusicServiceRangeRequestTest.kt` (`googleVideoBoundedLength`: `clen`→longitud restante; no toca requests ya acotados, agotados ni otros hosts); `MusicRepositoryDownloadIntegrationTest.googlevideoFirstRequest_sendsPartialChunksNotWholeFile`; `UserAgentMediaSourceFactoryTest.kt` (placeholder Remote `Uri.EMPTY` no tira) |
| Radio local / engine / Deezer | `app/src/test/.../RadioEngineTest.kt`, `DeezerSimilarRadioTest.kt` |
| LB radio / CF JSON parse | `app/src/test/.../ListenBrainzRadioParseTest.kt` |
| LB Para Ti → PlayableItem | `app/src/test/.../MatchedLbPlaylistPlayableTest.kt` |
| CF match Local|Remote | `app/src/test/.../FetchAndMatchCfRecommendationsUseCaseTest.kt` |
| ActiveDownload cycle | `app/src/test/.../ActiveDownloadCycleTest.kt` |
| ActiveDownload codec / badge | `app/src/test/.../ActiveDownloadCodecTest.kt` |
| Coordinación descargas multi-owner | `app/src/test/.../service/ProcessDownloadCoordinatorTest.kt` (dos callers/id variants, targets de varias playlists exactamente una vez, cancel real, merge persist, límite global 3) |
| Runtime/lifetime descargas | `app/src/test/.../service/ProcessDownloadRuntimeTest.kt` (owner process, resume selectivo/todo, gate metered); `OnlineDownloadServiceLauncherTest.kt` (UIDT vs FGS); `CatalogDownloadFunctionalTest.downloadContinuesAfterActivityAndViewModelAreDestroyed` |
| Transfer I/O | `app/src/test/.../data/util/TransferIoTest.kt`; integración música `MusicRepositoryDownloadIntegrationTest`; APK `ApkUpdateDownloaderTest` |
| Last-played / idle / queue hydrate | `app/src/test/.../PlaybackSessionStoreTest.kt` (incl. Remote-only + biblioteca vacía + duplicados) |
| System resumption | `PlaybackSystemResumptionTest.kt`; mutex/idempotencia en `PlaybackRuntimeContinuityTest.kt`; manifest `MediaButtonReceiver` |
| Browse Auto/AVRCP | `MediaLibraryIdsTest.kt`, `MediaLibraryBrowseMapperTest.kt`, `BrowseLocalLibraryUseCaseTest.kt`; `MediaLibraryBrowseInstrumentedTest.kt` |
| Queue wrap / trim / shuffle índices | `app/src/test/.../PlaybackQueueOrderTest.kt` |
| Queue snapshot codec | `app/src/test/.../QueueSnapshotCodecTest.kt` |
| Shuffle/repeat restore | `app/src/test/.../PlaybackModeRestoreTest.kt` |
| Shuffle/repeat clear-on-play | `app/src/test/.../PlaybackModeClearTest.kt` |
| Library UI prefs codec / nav | `app/src/test/.../LibraryUiPreferencesCodecTest.kt`, `PlaylistDetailNavTest.kt` |
| Audio persist canonicalize | `app/src/test/.../AudioPersistRefTest.kt` |
| Import LB playlist | `app/src/test/.../ImportListenBrainzPlaylistUseCaseTest.kt` |
| Path normalize | `app/src/test/.../SongPathNormalizerTest.kt` (incl. tree-only SAF URI) |
| Album art scan cache | `app/src/test/.../AlbumArtworkCacheTest.kt` |
| Filename metadata hints | `app/src/test/.../FilenameMetadataHintsTest.kt` (`parse` rip formats + `resolveWeak` + `looksLikeStoragePath`) |
| Audio tag reader | `app/src/test/.../AudioTagReaderTest.kt` |
| Embedded tag fill | `app/src/test/.../EmbeddedTagFillTest.kt` |
| Identify gaps | `app/src/test/.../IdentifyGapsTest.kt` (`gapApplyFields` / `needsGapIdentify` / título basura vs ID3) |
| Identify pipeline | `app/src/test/.../domain/usecase/IdentifyPipelineTest.kt` |
| Identify ranking | `app/src/test/.../IdentifyRankingTest.kt` |
| MusicBrainz parse | `app/src/test/.../MusicBrainzClientTest.kt` / `MusicBrainzClientIntegrationTest.kt` |
| Identify apply diffs | `app/src/test/.../IdentifyApplyChangesTest.kt` |
| Identify review search seed | `app/src/test/.../IdentifyReviewStateTest.kt` (también `Song.forIdentifyReview` sin LRC; leftover `isApplying` false; leftover no pisa chips; `identifyPersistEcho` skip/extras) |
| Identify work / runtime | `app/src/test/.../IdentifyWorkCodecTest.kt`, `IdentifyWorkStoreIntegrationTest.kt`, `ProcessIdentifyRuntimeTest.kt`, `IdentifyExecutionLauncherTest.kt` |
| Audio tag writer | `app/src/test/.../AudioTagWriterTest.kt` |
| Identify album groups | `app/src/test/.../IdentifyAlbumGroupsTest.kt` |
| Identify album batch resolver | `app/src/test/.../IdentifyAlbumBatchResolverTest.kt` |
| Identify known albums | `app/src/test/.../IdentifyKnownAlbumsTest.kt` |
| Identify review codec / hydrate | `app/src/test/.../IdentifyReviewCodecTest.kt` |
| GitHub release parser | `app/src/test/.../GitHubReleaseParserTest.kt` |
| Update download / VM | `app/src/test/.../data/update/ApkUpdateDownloaderTest.kt` (HTTP 500, truncado, validación/publicación) y `app/src/test/.../ui/update/AppUpdateViewModelTest.kt` (throttle, available, cache offline, error) |
| TrackIdentity JSON | `app/src/test/.../TrackIdentityJsonTest.kt` |
| TrackIdentity merge / toIdentity / youtubeSearchQuery / preferMetaFrom | `app/src/test/.../TrackIdentityTest.kt` |
| Pending mapper album/trackNumber↔entity | `app/src/test/.../PlaylistPendingTrackMapperTest.kt` |
| Match LB tracks / similar preview / download use case | `MatchListenBrainzTracksUseCaseTest.kt`, `BuildSimilarPlaylistPreviewUseCaseTest.kt`, `DownloadAudioTrackUseCaseTest.kt` |
| Listen sync queue | `ListenSyncCoordinatorTest.kt` |
| WiFi upload name sanitize | `UploadNameSanitizerTest.kt` |
| WiFi FGS timeout / Job stop | `WebServerServiceTimeoutTest.kt`, `OnlineDownloadServiceSupportTest.kt` |
| Stereo PCM pipeline | `StereoBalanceAudioProcessorTest.kt` |
| Estabilidad y telemetría | `SystemStabilityMonitorTest.kt` (mapeo razones exit, importancia, niveles trim de memoria y opt-out); `TelemetryPreferencesRepositoryTest.kt` (persistencia DataStore/SharedPreferences y toggle CrashReporter) |
| Library execute filter/sort / album override projection | `GetLibrarySongsUseCaseExecuteTest.kt`, `GetLibrarySongsUseCaseListItemsTest.kt` (ALBUM_GROUPS = extractAlbums), `AlbumCoverVsPlaylistCoverTest.kt` |
| Synced lyrics parse/format | `app/src/test/.../data/util/SyncedLyricsTest.kt` |
| Owners UI tipados | `ui/state/UiStateFlowsTest.kt`, `LibraryProjectionStateTest.kt` (list items/Recientes off-Main; overlay freeze), `LoadableUiStateTest.kt`, `CatalogUiStateTest.kt`, `UiNavigationStateTest.kt` |
| UI functional library | `app/src/androidTest/.../LibraryScreenFunctionalTest.kt`, `LibraryBrowseFunctionalTest.kt` |
| UI functional Settings / update | `app/src/androidTest/.../ui/settings/SettingsRuntimeFunctionalTest.kt` (tema recreate, modos persistidos, UI sonido→servicio real); `ui/update/AppUpdateFunctionalTest.kt` (GitHub mock→descarga→boundary instalador; truncado sin intent) y `PackageManagerApkValidatorInstrumentedTest.kt` (APK instalado del mismo package vs bytes inválidos) |
| UI functional downloads / remote row / cola duplicada | `DownloadsUiFunctionalTest.kt`, `PlaylistRemoteRowFunctionalTest.kt`, `QueueIdentityFunctionalTest.kt`; `ui/download/CatalogDownloadFunctionalTest.kt` (happy path + Overwrite/SaveAs/Cancel + cancel parcial + 403/retry + metered tras permit) |
| SAF / permisos instrumentados | Provider solo-debug `src/debug/AndroidManifest.xml` + `src/debug/java/.../testutil/TestAudioDocumentsProvider.kt` (mismo UID target; nunca entra al release); `data/repository/SafImportFunctionalTest.kt` (import/dedupe/playback + portada efímera), `ui/SafFolderPickerFunctionalTest.kt`, `ui/LibraryPermissionFunctionalTest.kt`; denied host-only `ui/LibraryPermissionDeniedHostE2ETest.kt` + `scripts/run-library-permission-denied-e2e.sh` |
| Discover / Radio instrumentados | `ui/discover/DiscoverE2EFunctionalTest.kt` + `DiscoverE2ETestFixture.kt` (LB/CF Local+Remote, descarga/rematch, guardar al escuchar→Room); `RadioModeControlFunctionalTest.kt`; runtime stop/refill/NEW en `PlaybackRuntimeContinuityTest.kt` |
| Continuidad instrumentada | `PlaybackContinuityFunctionalTest.kt` (pause durante resolve) |
| Lifecycle host / audio | `testutil/PlaybackDeviceProbe.kt` centraliza controller/notif/FGS/shell/main; `PlaybackProcessDeathE2ETest` + host script; `PlaybackTaskRemovalE2ETest`; `LockedScreenPlaybackFunctionalTest`; `PlaybackAudioInterruptionFunctionalTest`; `BackupRestoreE2ETest`; `DatabaseDowngradeWarningFunctionalTest` |
| E2E playlists / Identify / servicios concurrentes | `app/src/androidTest/.../ui/playlist/PlaylistCrudE2ETest.kt`, `PlaylistPendingDownloadE2ETest.kt`; `ui/identify/IdentifyE2EFunctionalTest.kt` (`identifyContinuesAfterActivityAndViewModelAreDestroyed`); `service/concurrent/ConcurrentServiceOperationsE2ETest.kt` |

## Símbolos ViewModel frecuentes

Mantener esta lista alineada con `MusicPlayerViewModel.kt`:

- Biblioteca/nav: `libraryProjection.{songs,albums,songList,artists,genres}`, `navigation` (`UiNavigationState`), `searchQuery`, `sortOption`, `setSortOption`, `sortDirection`, `setSortDirection` / `toggleSortDirection`, `libraryViewMode`, `setLibraryViewMode`, `toggleLibraryViewMode`, `setLibraryBrowseFilter`, `openLibraryAlbum`, `openLibraryArtist`, `openLibraryGenre`, `popLibraryNested`, `setSelectedNavIndex`, `openDownloadsTabTransient`, `openLocalPlaylist`, `openListenBrainzPlaylistDetail`, `openCfRecommendationsDetail`, `closePlaylistDetail`, `dismissDiscoverDetails`, `buildLibraryListModel`, `buildLibraryListItems`, `sortSongsWithinAlbum`, `songsForAlbum`, `songsFromLibraryListItems`, `songsInOrder`, `songsForBrowseProjection`, `playCurrentLibraryBrowse`, `currentSongId`, `libraryJobProgress`, `importFolder`, `ensureInitialLibraryImport`, `identifySongs` (fachada `ProcessIdentifyRuntime.submit` + `fillGapsOnly`), `cancelIdentify`, `identifySongForReview`, `setIdentifyReviewApplyFields`, `identifyReview`, `previewIdentifyLocalSong`, `previewIdentifyCandidate`, `applySelectedIdentifyCandidate` / `applyKnownAlbumFanOut`, `skipIdentifyReviewItem`, `searchIdentifyCandidates`, `loadMoreIdentifyCandidates`, `dismissIdentifyReview`, `showIdentifyReview`, `applyRemainingIdentifySuggestions`, `skipAllIdentifyReview`, `applyIdentifyAlbumGroup`, `startIdentifyItemReview`, `returnIdentifyReviewOverview`, `requestOpenIdentifyReview`, `updateSongLyrics`, `fetchSongLyrics`; lifecycle/settings `backgroundExecutionStatus`, `onAppForeground` (resume downloads + identify), `attachPlaybackUi`, `openPlaybackSettings`, `openDownloadSettings`, `telemetryEnabled`, `setTelemetryEnabled`
- Playback façade: `playSong`, `playCollection`, `playPlayableCollection`, `playMatchedCollection`, `shufflePlayableCollection`, `shuffleCollection`, `enqueueCollection`, `playNextInQueue`, `playNextBatch`, `skipToQueueIndex`, `moveDisplayQueueItem`, `togglePlayPause`, `seekTo` / `seekToAndPlay`, `toggleShuffle`, `toggleRepeatMode`; `currentItem`, `currentSong`, `queue`, `displayQueue`, `discoverPlaybackOrigin`, `resolvingRemote`, `repeatMode`, `isShuffle`, `queueFocusEpoch` son flows del runtime; VM conserva sonido/flags de preferencias y `playlistsContainingSong`
- Radio façade: `startRadio` / `stopRadio` / `setRadioPreferredMode`, `radioMode`, `radioStatusLabel`; implementación/refill/auto en `PlaybackRuntime`
- Artwork: `setAlbumArtwork`, `requestSaveAlbumMetadata`, `saveAlbumOverride` (private), `confirmPendingAlbumMerge`, `dismissPendingAlbumMerge`, `mergeAlbumInto`
- Online: `catalogSearch` / `catalogCollection`; `searchCatalog` / `searchOnlineCatalog` (latest-wins), categorías/colecciones, `downloadSingleCandidate`, `downloadSelectedCandidatesBatch`, `enqueueTrackedBatch`, `downloadFromUrl`, `downloadOnlineTrack`; `activeDownloads`/`downloadConflict` vienen de `app.processDownloadRuntime`; acciones `retryActiveDownload` / `resumeAllDownloads` / cycle/preview/play/dismiss; conflicto delega runtime; `runTrackedDownload` adapta request. Ajustes/deep-links/preview permanecen en VM
- ListenBrainz: `tokenValidation`, `lbDiscover`, `lbPlaylistDetail`, `cfRecommendations` (`LoadableUiState`); `listenBrainzSettings`, `setListenBrainzEnabled`, `setListenBrainzDiscoverEnabled`, `setListenBrainzSaveWhileListening`, `setListenBrainzSaveWhileListeningPercent`, `refreshListenBrainzDiscoverPlaylists`, `openListenBrainzPlaylist`, `openListenBrainzPlaylistDetail`, `playMatchedTracks`, `shuffleMatchedTracks`, `saveListenBrainzPlaylistAsLocal`, `importListenBrainzPlaylistWithDownloads`, `downloadPlaylistPendingTracks`, `downloadRemoteItem`, `getPlaylistPendingTracksFlow`, `refreshCfRecommendations`, `openCfRecommendations`, `openCfRecommendationsDetail`, `closePlaylistDetail`, `navigation.playlistDetail`, `discoverPlaybackOrigin`

## Cómo actualizar este mapa

Tras crear/renombrar/mover un archivo o API pública relevante:

1. Editar la fila correspondiente (o añadir sección).
2. Si el change afecta un feature, actualizar también `bestiapop-features`.
3. Si cambia capas/paquetes/stack, actualizar `bestiapop-architecture`.
